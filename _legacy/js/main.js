$(document).ready(function(){

	// Menu-Hamburger
	// $('.menu-bars, .menu a, .mob-line').click(function () {
	// 	if ($(window).width() <= 991) {
	// 		$('.menu-open').toggleClass('d-none');
	// 	}
    // });

    // Smooth movement
    // $("a.go").click(function (e) {
    //     e.preventDefault();
    //     elementClick = $(this).attr("href");
    //     destination = $(elementClick).offset().top;
    //     $("body,html").animate({scrollTop: destination }, 800);
    // });

    // Scroll Up
    // $(window).scroll(function() {
	// 	if($(this).scrollTop() != 0) {
	// 		$('#toTop').fadeIn();
	// 	} 
	// 	else {
	// 		$('#toTop').fadeOut();
	// 	}
	// });
	// $('#toTop').click(function() {
	// 	$('body,html').animate({scrollTop:0},800);
	// });

	// HTML
	// <!-- Прокрутка вверх -->
	// <div id="toTop">
	// 	<i class="fa fa-arrow-up" aria-hidden="true"></i>
	// </div>

    // Slick slider
	// if ($(window).width() <= 991) {
	//   	$('.recom-img-wrap').slick({
	//    	dots: true,
	//    	arrows: false
	//   	});
	//   	$('.gallery-wrap').slick({
	//    	dots: true,
	//    	arrows: false
	//   	});

	//   	$('.kinds-card').removeClass('kinds-card-hover');
	// }

});